const users = require("./users");
const libraries =require("./libraries");
const favorites =require("./favorites");

module.exports = {
    users: users,
    libraries:libraries,
    favorites:favorites
};